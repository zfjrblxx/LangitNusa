import {MapPin, Clock3} from 'lucide-react'
import {SectionHead} from './Weather'
export default function Earthquake({quake,loading}){const g=quake?.Infogempa?.gempa;return <section className="section" id="earthquake"><SectionHead title="Gempa bumi terbaru" note="Peristiwa terbaru · BMKG"/><div className="earthquake"><div className="mag">{g?`M ${g.Magnitude}`:'—'}</div><div className="quake-info">{loading?<div className="muted">Memuat data gempa…</div>:g?<><div className="quake-place">{g.Wilayah}</div><div className="quake-meta"><Clock3 size={13}/>{g.Tanggal} · {g.Jam}</div><div className="quake-meta"><MapPin size={13}/>{g.Kedalaman} · {g.Potensi||'Potensi tidak tercantum'}</div></>:<div className="error">Data gempa sementara tidak tersedia.</div>}</div></div></section>}
